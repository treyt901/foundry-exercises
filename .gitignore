# .env ships with the assignment (students fill it in on their own box).
# Instructors: never commit it with real values.

# Runtime files created by lab.sh / the Flask app.
.flask.pid
.flask.log

# Per-student grading results written by the Prompt Lab.
results/

# Python
__pycache__/
*.pyc
